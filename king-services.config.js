/**
 * king-services.config.js — centralized service configuration
 *
 * Controls:
 *   - Watchdog behavior (polling interval, restart policy)
 *   - Health check timeouts
 *   - Auto-compact settings (conversation TTL, batch size)
 *   - Feature flags (best features, optimizations)
 */

module.exports = {
  // ─── Watchdog ─────────────────────────────────────────────────────────────
  watchdog: {\n    pollMs: 30000,                  // Check services every 30s\n    restartDelayMs: 5000,           // Wait 5s between restart attempts\n    unhealthyThresholdMs: 8000,     // Mark unhealthy after 8s no response\n    gracefulShutdownMs: 15000,      // Wait 15s for graceful shutdown\n    maxRestarts: 5,                 // Restart up to 5 times before giving up\n  },\n\n  // ─── HTTP Health Checks ───────────────────────────────────────────────────\n  healthChecks: {\n    timeout: 4000,                  // 4s per check\n    retries: 12,                    // Retry 12 times (24s total) on startup\n    retryDelayMs: 2000,             // Wait 2s between retries\n    persistentCheckMs: 300000,      // Every 5 minutes for persistent checks\n  },\n\n  // ─── Services ─────────────────────────────────────────────────────────────\n  services: {\n    docker: [\n      {\n        id: 'studio-assets',\n        containers: ['studio-assets-studio-assets-1', 'studio-assets-studio-neo4j-1', '12sgi-king-auth-1'],\n        healthUrls: {\n          assets: 'http://localhost:8108/api/v2/ready',\n          auth: 'http://localhost:8101/api/v2/ready',\n        },\n      },\n    ],\n    process: [\n      {\n        id: 'king-bridge',\n        cmd: 'python',\n        args: [\n          '-m', 'uvicorn',\n          'services.king_bridge.app.main:app',\n          '--host', '127.0.0.1',\n          '--port', '8109',\n          '--workers', '2',\n          '--log-level', 'info',\n        ],\n        env: {\n          PYTHONUNBUFFERED: '1',\n          NEO4J_CONV_AUTO_COMPACT: 'true',\n          NEO4J_CONV_TTL_DAYS: '30',\n          NEO4J_CONV_BATCH_SIZE: '1000',\n          OLLAMA_HOST: 'http://localhost:11434',\n        },\n        healthUrl: 'http://localhost:8109/api/v2/ready',\n      },\n      {\n        id: 'static-server',\n        cmd: 'python',\n        args: [\n          '-m', 'http.server',\n          '8888',\n          '--directory', './',\n        ],\n        healthUrl: 'http://localhost:8888/',\n      },\n    ],\n    optional: [\n      {\n        id: 'ollama',\n        cmd: 'ollama',\n        args: ['serve'],\n        healthUrl: 'http://localhost:11434/api/tags',\n        required: false,  // Gracefully degrade without Ollama\n        description: 'Local inference engine (LLaMA, Mistral, etc.)',\n      },\n    ],\n  },\n\n  // ─── Neo4j Conversation Auto-Compact ──────────────────────────────────────\n  autoCompact: {\n    enabled: true,\n    ttlDays: 30,                    // Compact conversations older than 30 days\n    batchSize: 1000,                // Process 1000 records per batch\n    scheduleHour: 2,                // Run at 2 AM local time\n    archiveDir: './data/archived-conversations',\n    retentionLevels: {\n      production: 90,               // Production: 90 days\n      staging: 30,                  // Staging: 30 days\n      development: 7,               // Dev: 7 days\n    },\n  },\n\n  // ─── Best Features / Optimizations ────────────────────────────────────────\n  features: {\n    'persistent-session': true,     // Resume conversation across restarts\n    'conversation-cache': true,     // Cache Neo4j query results\n    'background-indexing': true,    // Index characters/tenants in background\n    'health-dashboard': true,       // Export /api/v2/health metrics\n    'graceful-degradation': true,   // Continue without Ollama, fallback to API\n    'request-compression': true,    // gzip responses > 1KB\n    'cors-enabled': true,           // Allow cross-origin requests (for dashboards)\n    'rate-limiting': true,          // 100 req/min per IP\n    'request-logging': true,        // Log all API calls\n    'performance-tracing': true,    // OpenTelemetry spans\n  },\n\n  // ─── Tenant Registry (read-only) ──────────────────────────────────────────\n  tenants: {\n    films: 9,\n    games: 1,\n    musicVideos: 2,\n    civic: 1,\n    total: 13,\n  },\n\n  characters: {\n    named: 25,\n    lipSyncSkills: 6,\n    renderStyles: 5,\n    civicDivisions: 12,\n  },\n\n  // ─── Logging ──────────────────────────────────────────────────────────────\n  logging: {\n    format: 'json',                 // 'json' or 'text'\n    level: 'info',                  // 'debug', 'info', 'warn', 'error'\n    file: './watchdog.log',\n    maxSizeBytes: 50 * 1024 * 1024, // 50 MB\n    maxFiles: 5,                    // Keep 5 rotated logs\n    colorize: true,                 // Terminal colors\n  },\n};\n