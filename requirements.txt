# Kilo Aupuni watchers are stdlib-only except for PDF extraction (agendas/minutes/packets).
pypdf>=4.0

# Neo4j Python driver — required for AuraDB Free cloud sync (watchers/aura_sync.py)
# and king-bridge AuraDB fallback. Not required for local-only Neo4j HTTP use.
# Install: pip install neo4j
neo4j>=5.0,<6.0
