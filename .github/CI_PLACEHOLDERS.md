# CI placeholders

These workflows are placeholders to remind contributors to add lint/test/accessibility/secret-scan checks to the app/service CI.

They are intentionally workflow_dispatch-only and do not run on push.
