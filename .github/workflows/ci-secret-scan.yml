name: CI - secret scan placeholder
on:
  workflow_dispatch: {}
jobs:
  secret-scan:
    runs-on: ubuntu-latest
    steps:
      - name: Placeholder
        run: echo "This workflow is a secret-scan placeholder. Add trufflehog/gitleaks steps here."
