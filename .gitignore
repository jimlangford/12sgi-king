# SECRETS — never commit
legiscan_key.txt
*.key
.env
<<<<<<< main
config/*
!config/owner_policy.json
!config/platform_version.json
!config/tenant_registry.v2.json
!config/tenant_transition_schema.v2.json
!config/canonical_job_contract.v2.json
!config/gpu_profiles/
!config/gpu_profiles/rtx4070_8gb.json
!config/ai_tool_registry.v2.json
!config/ai_profiles/
!config/ai_profiles/*.json
=======
.env.v2
config/
>>>>>>> origin/main

# generated locally (CI builds it fresh; deploy uploads it)
site/

# python cruft + watcher state
__pycache__/
*.pyc
*_state.json
_probe*.py

# never publish the raw heavy archives by accident
watchers/*_state.json

# internal cross-thread coordination bus — never publish (not leak-scanned; keep private)
.dispatch_log.jsonl

# connector audit/intent logs (post text + timestamps) — PRIVATE, not leak-scanned
# (2026-07-11, own_channel_post.py + connect_post.py both write here)
reports/_status/*.jsonl

# SECURITY (Jimmy 2026-06-26 harden): private corporate data room / cap table / investor / charter - NEVER in the public repo
king_private_src/

# git filter-branch / filter-repo rewrite artifacts — never commit these
.git-rewrite/

# runtime SQLite databases — generated locally, never commit
.platform_events.db
*.db
